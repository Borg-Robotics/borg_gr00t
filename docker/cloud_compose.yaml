services:
  borg-gr00t:
    image: davideborg/borg-gr00t:dev
    container_name: borg-gr00t
    runtime: nvidia
    network_mode: host

    volumes:
      - ../:/workspace

    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities:
              - gpu
              - utility
              - compute
              - video

    entrypoint: bash
    stdin_open: true
    tty: true
